diff --git a/node_modules/iwer/lib/device/XRDevice.d.ts b/node_modules/iwer/lib/device/XRDevice.d.ts
index f3214dc..5edf2c4 100644
--- a/node_modules/iwer/lib/device/XRDevice.d.ts
+++ b/node_modules/iwer/lib/device/XRDevice.d.ts
@@ -26,7 +26,7 @@ import { NativePlane } from '../planes/XRPlane.js';
 import { NativeMesh } from '../meshes/XRMesh.js';
 import type { DepthSensingData } from '../depth/XRDepthInformation.js';
 import type { XRRuntimeAdapter } from '../types/runtime-session.js';
-export type WebXRFeature = 'viewer' | 'local' | 'local-floor' | 'bounded-floor' | 'unbounded' | 'dom-overlay' | 'anchors' | 'plane-detection' | 'mesh-detection' | 'hit-test' | 'hand-tracking' | 'depth-sensing';
+export type WebXRFeature = 'viewer' | 'local' | 'local-floor' | 'bounded-floor' | 'unbounded' | 'dom-overlay' | 'anchors' | 'plane-detection' | 'mesh-detection' | 'hit-test' | 'hand-tracking' | 'depth-sensing' | 'light-estimation' | 'webgpu';
 export interface XRDeviceConfig {
     name: string;
     controllerConfig: XRControllerConfig | undefined;
@@ -119,6 +119,14 @@ export declare class XRDevice {
         stereoEnabled: boolean;
         ipd: number;
         fovy: number;
+        nativeProjection: Partial<Record<XREye, Float32Array>>;
+        nativeViewports: Partial<Record<XREye, {
+            x: number;
+            y: number;
+            width: number;
+            height: number;
+        }>>;
+        onFrameEnd: ((frame: XRFrame) => void) | undefined;
         controllers: {
             [key in XRHandedness]?: XRController;
         };
@@ -169,7 +177,7 @@ export declare class XRDevice {
         controlMode: ControlMode;
         controlModeListeners: Set<(mode: ControlMode) => void>;
         stateChangeListeners: Set<() => void>;
-        remote: RemoteControlInterface;
+        remote: RemoteControlInterface | null;
         runtime: XRRuntimeAdapter;
         lastFrameTime: number;
     };
@@ -201,6 +209,13 @@ export declare class XRDevice {
     set ipd(value: number);
     get fovy(): number;
     set fovy(value: number);
+    get nativeProjection(): Partial<Record<XREye, Float32Array>>;
+    get nativeViewports(): Partial<Record<XREye, {
+        x: number;
+        y: number;
+        width: number;
+        height: number;
+    }>>;
     get position(): Vector3;
     get quaternion(): Quaternion;
     get viewerSpace(): XRReferenceSpace;
@@ -243,7 +258,7 @@ export declare class XRDevice {
     createActionPlayer(refSpace: XRReferenceSpace, recording: CompressedRecording, options?: ActionPlayerOptions): ActionPlayer;
     get devui(): DevUI | undefined;
     get sem(): SyntheticEnvironmentModule | undefined;
-    get remote(): RemoteControlInterface;
+    get remote(): RemoteControlInterface | null;
     /**
      * Get the current control mode
      * - 'manual': User controls device via DevUI (default)
diff --git a/node_modules/iwer/lib/device/XRDevice.js b/node_modules/iwer/lib/device/XRDevice.js
index 9c561cd..0818bd5 100644
--- a/node_modules/iwer/lib/device/XRDevice.js
+++ b/node_modules/iwer/lib/device/XRDevice.js
@@ -7,7 +7,7 @@
 import { GlobalSpace, XRSpace } from '../spaces/XRSpace.js';
 import { P_DEVICE, P_REF_SPACE, P_SESSION, P_SPACE, P_SYSTEM, } from '../private.js';
 import { Quaternion, Vector3 } from '../utils/Math.js';
-import { RemoteControlInterface } from '../remote/RemoteControlInterface.js';
+// HOLOWEB: remote control interface removed from the core bundle
 import { XRController } from './XRController.js';
 import { XRSession, } from '../session/XRSession.js';
 import { XREye, XRView } from '../views/XRView.js';
@@ -105,6 +105,10 @@ export class XRDevice {
             stereoEnabled: (_d = deviceOptions.stereoEnabled) !== null && _d !== void 0 ? _d : DEFAULTS.stereoEnabled,
             ipd: (_e = deviceOptions.ipd) !== null && _e !== void 0 ? _e : DEFAULTS.ipd,
             fovy: (_f = deviceOptions.fovy) !== null && _f !== void 0 ? _f : DEFAULTS.fovy,
+            // HOLOWEB: per-eye projection / viewport overrides supplied by the host
+            nativeProjection: {},
+            nativeViewports: {},
+            onFrameEnd: undefined,
             controllers,
             hands,
             primaryInputMode: 'controller',
@@ -152,6 +156,11 @@ export class XRDevice {
                         vpHeight = height;
                         break;
                 }
+                // HOLOWEB: host-supplied viewport rect (framebuffer pixels) wins
+                const nativeViewport = this[P_DEVICE].nativeViewports[eye];
+                if (nativeViewport) {
+                    ({ x, y, width: vpWidth, height: vpHeight } = nativeViewport);
+                }
                 // cache per (eye, width, height, stereoEnabled): return the cached
                 // instance unless one of those changed
                 const cached = this[P_DEVICE].viewportCache[eye];
@@ -313,7 +322,7 @@ export class XRDevice {
                 };
             },
         };
-        this[P_DEVICE].remote = new RemoteControlInterface(this);
+        this[P_DEVICE].remote = null;
         this[P_DEVICE].updateViews();
         globalThis;
     }
@@ -511,6 +520,13 @@ export class XRDevice {
     get fovy() {
         return this[P_DEVICE].fovy;
     }
+    // HOLOWEB: per-eye overrides, see XRSession onDeviceFrame and getViewport
+    get nativeProjection() {
+        return this[P_DEVICE].nativeProjection;
+    }
+    get nativeViewports() {
+        return this[P_DEVICE].nativeViewports;
+    }
     set fovy(value) {
         this[P_DEVICE].fovy = value;
     }
diff --git a/node_modules/iwer/lib/frameloop/XRFrame.js b/node_modules/iwer/lib/frameloop/XRFrame.js
index 01e78cc..f958cf3 100644
--- a/node_modules/iwer/lib/frameloop/XRFrame.js
+++ b/node_modules/iwer/lib/frameloop/XRFrame.js
@@ -80,7 +80,10 @@ export class XRFrame {
         const session = this[P_FRAME].session;
         const device = session[P_SESSION].device;
         const pose = this.getPose(device.viewerSpace, referenceSpace);
-        const eyes = session[P_SESSION].mode === 'inline' ? INLINE_EYES : STEREO_EYES;
+        // HOLOWEB: a mono immersive session (handheld AR) has one 'none' view
+        const eyes = session[P_SESSION].mode === 'inline' || !device.stereoEnabled
+            ? INLINE_EYES
+            : STEREO_EYES;
         const views = [];
         eyes.forEach((eye) => {
             const viewSpace = device.viewSpaces[eye];
diff --git a/node_modules/iwer/lib/hittest/XRRay.js b/node_modules/iwer/lib/hittest/XRRay.js
index eafdd9e..f99b672 100644
--- a/node_modules/iwer/lib/hittest/XRRay.js
+++ b/node_modules/iwer/lib/hittest/XRRay.js
@@ -72,7 +72,8 @@ export class XRRay {
         const z = vec3.set(vec3.create(), 0, 0, -1);
         const origin = vec3.set(vec3.create(), this[P_RAY].origin.x, this[P_RAY].origin.y, this[P_RAY].origin.z);
         const direction = vec3.set(vec3.create(), this[P_RAY].direction.x, this[P_RAY].direction.y, this[P_RAY].direction.z);
-        const axis = vec3.cross(vec3.create(), direction, z);
+        // HOLOWEB: rotate -Z onto direction (upstream used direction x z, which flips non-default rays)
+        const axis = vec3.cross(vec3.create(), z, direction);
         const cosAngle = vec3.dot(direction, z);
         const rotation = mat4.create();
         if (cosAngle > -1 && cosAngle < 1) {
diff --git a/node_modules/iwer/lib/session/XRRenderState.d.ts b/node_modules/iwer/lib/session/XRRenderState.d.ts
index cb77fb2..b14f9d1 100644
--- a/node_modules/iwer/lib/session/XRRenderState.d.ts
+++ b/node_modules/iwer/lib/session/XRRenderState.d.ts
@@ -5,24 +5,27 @@
  * LICENSE file in the root directory of this source tree.
  */
 import { P_RENDER_STATE } from '../private.js';
-import { XRWebGLLayer } from '../layers/XRWebGLLayer.js';
+import { XRLayer, XRWebGLLayer } from '../layers/XRWebGLLayer.js';
 export declare class XRRenderState {
     [P_RENDER_STATE]: {
         depthNear: number;
         depthFar: number;
         inlineVerticalFieldOfView: number | null;
         baseLayer: XRWebGLLayer | null;
+        layers: XRLayer[] | undefined;
     };
     constructor(init?: Partial<XRRenderStateInit>, oldState?: XRRenderState);
     get depthNear(): number;
     get depthFar(): number;
     get inlineVerticalFieldOfView(): number | null;
     get baseLayer(): XRWebGLLayer | null;
+    get layers(): XRLayer[];
 }
 export interface XRRenderStateInit {
     depthNear?: number;
     depthFar?: number;
     inlineVerticalFieldOfView?: number;
     baseLayer?: XRWebGLLayer;
+    layers?: XRLayer[];
 }
 //# sourceMappingURL=XRRenderState.d.ts.map
\ No newline at end of file
diff --git a/node_modules/iwer/lib/session/XRRenderState.js b/node_modules/iwer/lib/session/XRRenderState.js
index 80a3bc7..eb95e38 100644
--- a/node_modules/iwer/lib/session/XRRenderState.js
+++ b/node_modules/iwer/lib/session/XRRenderState.js
@@ -14,7 +14,9 @@ export class XRRenderState {
             depthNear: (_b = (_a = init.depthNear) !== null && _a !== void 0 ? _a : oldState === null || oldState === void 0 ? void 0 : oldState.depthNear) !== null && _b !== void 0 ? _b : 0.1,
             depthFar: (_d = (_c = init.depthFar) !== null && _c !== void 0 ? _c : oldState === null || oldState === void 0 ? void 0 : oldState.depthFar) !== null && _d !== void 0 ? _d : 1000.0,
             inlineVerticalFieldOfView: (_f = (_e = init.inlineVerticalFieldOfView) !== null && _e !== void 0 ? _e : oldState === null || oldState === void 0 ? void 0 : oldState.inlineVerticalFieldOfView) !== null && _f !== void 0 ? _f : null,
-            baseLayer: (_h = (_g = init.baseLayer) !== null && _g !== void 0 ? _g : oldState === null || oldState === void 0 ? void 0 : oldState.baseLayer) !== null && _h !== void 0 ? _h : null,
+            // HOLOWEB: WebXR Layers support (XRGPUBinding projection layers)
+            layers: init.layers !== undefined ? Array.from(init.layers) : init.baseLayer ? [] : oldState === null || oldState === void 0 ? void 0 : oldState.layers,
+            baseLayer: init.layers !== undefined ? null : (_h = (_g = init.baseLayer) !== null && _g !== void 0 ? _g : oldState === null || oldState === void 0 ? void 0 : oldState.baseLayer) !== null && _h !== void 0 ? _h : null,
         };
     }
     get depthNear() {
@@ -29,5 +31,9 @@ export class XRRenderState {
     get baseLayer() {
         return this[P_RENDER_STATE].baseLayer;
     }
+    get layers() {
+        var _a;
+        return (_a = this[P_RENDER_STATE].layers) !== null && _a !== void 0 ? _a : [];
+    }
 }
 //# sourceMappingURL=XRRenderState.js.map
\ No newline at end of file
diff --git a/node_modules/iwer/lib/session/XRSession.js b/node_modules/iwer/lib/session/XRSession.js
index de7bbb7..65ca617 100644
--- a/node_modules/iwer/lib/session/XRSession.js
+++ b/node_modules/iwer/lib/session/XRSession.js
@@ -20,6 +20,18 @@ import { XREye } from '../views/XRView.js';
 import { XRRigidTransform } from '../primitives/XRRigidTransform.js';
 import { XRFrame } from '../frameloop/XRFrame.js';
 import { mat4 } from 'gl-matrix';
+// HOLOWEB: copy a host projection and rebuild its depth terms from depthNear/depthFar
+const applyNativeProjection = (out, src, near, far) => {
+    out.set(src);
+    if (far === Infinity) {
+        out[10] = -1;
+        out[14] = -2 * near;
+    }
+    else {
+        out[10] = -(far + near) / (far - near);
+        out[14] = (-2 * far * near) / (far - near);
+    }
+};
 export var XREnvironmentBlendMode;
 (function (XREnvironmentBlendMode) {
     XREnvironmentBlendMode["Opaque"] = "opaque";
@@ -85,11 +97,15 @@ export class XRSession extends EventTarget {
                     this[P_SESSION].device[P_DEVICE].onBaseLayerSet(this[P_SESSION].renderState.baseLayer);
                 }
                 const baseLayer = this[P_SESSION].renderState.baseLayer;
-                if (baseLayer === null) {
+                // HOLOWEB: a session rendering through renderState.layers has no baseLayer
+                const layers = this[P_SESSION].renderState.layers;
+                if (baseLayer === null && layers.length === 0) {
                     return;
                 }
-                const context = baseLayer.context;
-                const canvas = context.canvas;
+                const context = baseLayer === null ? null : baseLayer.context;
+                const canvas = context === null
+                    ? { width: globalThis.innerWidth, height: globalThis.innerHeight }
+                    : context.canvas;
                 /**
                  * This code snippet is designed to clear the buffers attached to an opaque framebuffer
                  * at the beginning of each XR animation frame, but it only applies to immersive XR sessions.
@@ -118,16 +134,23 @@ export class XRSession extends EventTarget {
                  * prevent rendering artifacts from past frames. It ensures that each new frame starts
                  * with a clean slate.
                  */
-                if (this[P_SESSION].mode != 'inline') {
+                if (context !== null && this[P_SESSION].mode != 'inline') {
                     const currentClearColor = context.getParameter(context.COLOR_CLEAR_VALUE);
                     const currentClearDepth = context.getParameter(context.DEPTH_CLEAR_VALUE);
                     const currentClearStencil = context.getParameter(context.STENCIL_CLEAR_VALUE);
+                    // HOLOWEB: clear the whole framebuffer even if the page left a per-eye scissor enabled
+                    // (otherwise a former right-eye region survives a stereo -> mono switch)
+                    const scissorTest = context.isEnabled(context.SCISSOR_TEST);
+                    if (scissorTest)
+                        context.disable(context.SCISSOR_TEST);
                     context.clearColor(0.0, 0.0, 0.0, 0.0);
                     context.clearDepth(1);
                     context.clearStencil(0.0);
                     context.clear(context.DEPTH_BUFFER_BIT |
                         context.COLOR_BUFFER_BIT |
                         context.STENCIL_BUFFER_BIT);
+                    if (scissorTest)
+                        context.enable(context.SCISSOR_TEST);
                     context.clearColor(currentClearColor[0], currentClearColor[1], currentClearColor[2], currentClearColor[3]);
                     context.clearDepth(currentClearDepth);
                     context.clearStencil(currentClearStencil);
@@ -140,11 +163,20 @@ export class XRSession extends EventTarget {
                         height;
                     mat4.perspective(this[P_SESSION].projectionMatrices[XREye.Left], this[P_SESSION].device.fovy, aspect, depthNear, depthFar);
                     mat4.copy(this[P_SESSION].projectionMatrices[XREye.Right], this[P_SESSION].projectionMatrices[XREye.Left]);
+                    mat4.copy(this[P_SESSION].projectionMatrices[XREye.None], this[P_SESSION].projectionMatrices[XREye.Left]);
                 }
                 else {
                     const aspect = width / height;
                     mat4.perspective(this[P_SESSION].projectionMatrices[XREye.None], this[P_SESSION].renderState.inlineVerticalFieldOfView, aspect, depthNear, depthFar);
                 }
+                // HOLOWEB: host-supplied per-eye projections replace the symmetric default
+                const nativeProjection = this[P_SESSION].device[P_DEVICE].nativeProjection;
+                for (const eye of [XREye.Left, XREye.Right, XREye.None]) {
+                    const src = nativeProjection[eye];
+                    if (src) {
+                        applyNativeProjection(this[P_SESSION].projectionMatrices[eye], src, depthNear, depthFar);
+                    }
+                }
                 const now = performance.now();
                 const frame = new XRFrame(this, this[P_SESSION].frameHandle, true, true, now);
                 const devui = this[P_SESSION].device[P_DEVICE].devui;
@@ -198,6 +230,16 @@ export class XRSession extends EventTarget {
                     }
                 }
                 this[P_SESSION].currentFrameCallbacks = null;
+                // HOLOWEB: host hook after the app's callbacks (layer presentation)
+                const onFrameEnd = this[P_SESSION].device[P_DEVICE].onFrameEnd;
+                if (onFrameEnd) {
+                    try {
+                        onFrameEnd(frame);
+                    }
+                    catch (err) {
+                        console.error(err);
+                    }
+                }
                 // - Set frame’s active boolean to false.
                 frame[P_FRAME].active = false;
                 this[P_SESSION].activeFrame = null;
@@ -468,11 +510,16 @@ export class XRSession extends EventTarget {
         if (state.baseLayer && state.baseLayer[P_WEBGL_LAYER].session !== this) {
             throw new DOMException('Base layer was created by a different XRSession', 'InvalidStateError');
         }
+        // HOLOWEB: baseLayer and layers are mutually exclusive (WebXR Layers)
+        if (state.baseLayer && state.layers) {
+            throw new DOMException('baseLayer and layers cannot both be set', 'NotSupportedError');
+        }
         if (state.inlineVerticalFieldOfView != null &&
             this[P_SESSION].mode !== 'inline') {
             throw new DOMException('InlineVerticalFieldOfView must not be set for an immersive session', 'InvalidStateError');
         }
         const compoundStateInit = {
+            layers: (_a = state.layers) !== null && _a !== void 0 ? _a : (_b = this[P_SESSION].pendingRenderState) === null || _b === void 0 ? void 0 : _b.layers,
             // Use nullish coalescing so legitimate 0 values (e.g.
             // inlineVerticalFieldOfView=0) are preserved instead of being dropped.
             baseLayer: (_c = (_a = state.baseLayer) !== null && _a !== void 0 ? _a : (_b = this[P_SESSION].pendingRenderState) === null || _b === void 0 ? void 0 : _b.baseLayer) !== null && _c !== void 0 ? _c : undefined,
